specVersion: 0.0.5
schema:
  file: ./schema.graphql
dataSources:
  - kind: substreams
    name: TxStream
    network: mainnet
    source:
      package:
        module: map_transactions
        file: ../substreams/txs.spkg   # result of `substreams build`
    mapping:
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      file: ./src/mapping.ts
      entities:
        - Transaction
