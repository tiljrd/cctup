{
  "name": "cctup-subgraph",
  "version": "0.1.0",
  "description": "Subgraph for persisting EVM transaction records",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy",
    "create-local": "graph create --node http://localhost:8020/ cctup/indexer",
    "remove-local": "graph remove --node http://localhost:8020/ cctup/indexer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cctup/indexer"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.69.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "0.31.0",
    "assemblyscript": "^0.28.2",
    "typescript": "^5.0.0"
  }
}
